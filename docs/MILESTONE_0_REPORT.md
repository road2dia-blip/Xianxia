# MILESTONE 0 — Project foundation

Report in the charter §14.1 format. Written in the Claude Code remote container on 2026-09-04, branch `claude/new-session-zyrmnu`. The container has **no Unreal Engine** (D-0008): everything marked *owner* below is verified only when the owner follows `docs/OWNER_FIRST_RUN.md` and pastes the evidence into the placeholders in this file.

## VERIFIED

### What was confirmed, with the evidence
- **The 81-row ladder exists with plausible curves.** `Content/Ascension/Data/DT_RealmLadder.json` and `.csv`, generated by `Tools/Ladder/generate_realm_ladder.py` from the §6.3 constants; recomputed independently from the charter formulas plus D-0003–D-0007/D-0012 with **0 mismatches** in 81 rows; CSV agrees with JSON field-for-field; every `UDA_RealmLadderConfig` default equals the generator's default; a replica of the C++ `ComputeRow` arithmetic reproduced all 81 JSON rows. All rows are printed in `docs/CHANGELOG.md`. Preview Reach 800 / 1,184 / 1,520 / 5,487 / 37,637 / 201,085 at 1.1 / 1.9 / 2.1 / 4.1 / 7.1 / 9.9 (the last differs from the charter's prose "beyond 250,000"; constants kept, D-0007).
- **The Gameplay Tag hierarchy exists.** 171 tags under `Ascension.` (System 35, Qi.Condition 4, Qi.Nature 10, State 39, Event 37, Mantra 12, Dao 25, Resonance 6, Scar 3), byte-identical across `docs/SKELETON_M0.md`, `Source/Ascension/AscensionGameplayTags.h` (extern declarations), `AscensionGameplayTags.cpp` (definitions with DevComments) and `Config/DefaultGameplayTags.ini`; no duplicates; every `Ascension.*` literal used anywhere in `Source/`, `Config/` and `Content/Python` exists; all 57 native tag references in `RealmLadder.cpp` are declared.
- **The folder structure of §11.1 and the C++ module skeleton exist.** Runtime module `Ascension` (Cultivation/, Player/, UI/; 27 headers, 25 sources, 7,183 lines including its rules file) and editor module `AscensionEditor` (3 headers, 3 sources); 7,655 lines across `Source/` including the target and module rules files; every file, class, struct, member and entry point in the contract `docs/SKELETON_M0.md` is present (scripted check: 38 classes/structs, all ten §11.5 debug-panel functions on `UDebugPanelWidget`, `.generated.h` last and `GENERATED_BODY` in every reflected header). The pure calculations the milestone requires are implemented: `UModifierStack::Get/Rebuild`, `URealmLadderLibrary::ComputeRow/GenerateLadderRows`, `UBreakthroughLibrary::ComputeMinorChance/DescribeFailure`, `FSeededStream`. Gameplay bodies are stubs marked `TODO(M1)`…`TODO(M5)` (19/11/15/1/4 markers).
- **Syntax of every translation unit (model test, not a build).** `Tools/StubCompile/check.sh`: clang 18 `-std=c++20 -fsyntax-only` of every `.cpp` against hand-written stub engine headers, runtime module with `WITH_EDITOR=0` and `=1`, editor module with `WITH_EDITOR=1`, plus textual UHT-style lints — **53 PASS, 0 FAIL of 53; 0 clang errors; 0 lint failures** on the final tree.
- **Charter boundaries.** No "Law" as a progression term; no "mana", "energy" or "MP" in any `FText`, DevComment or Python user-facing string; no hardcoded key checks in `Source/`; no external downloads, plugins outside the engine, networking, combat or inventory; every asset path is under `/Game/Ascension/`.
- **`DA_RealmLadderConfig` formulas and the generation utility.** Constants as C++ defaults on `UDA_RealmLadderConfig` (mirrored in the Python twin); `URealmLadderLibrary` (runtime), `URealmLadderCommandlet -run=RealmLadder` and `URealmLadderEditorLibrary::RegenerateLadderTable` (editor) share one generator.
- **Git.** Repository initialised with an Unreal `.gitignore`. Branch `claude/new-session-zyrmnu` history at the time of writing: `8ff12ae` (the owner's initial empty-repository commit), `8195d04` (groundwork: charter, decisions, plan, ladder generator), then four work-in-progress commits `61639c5`, `2f45885`, `dafb9d3`, `f8698be` made because the build ran across usage-limit interruptions; the Milestone 0 commit follows this report on the same branch. The repository therefore holds more than the charter's "one commit" (`docs/MILESTONE_0_PLAN.md` §6 says the same); history was never rewritten and nothing was force-pushed (charter 2.3).
- **Adversarial review.** Four review lenses (charter/contract compliance, UE 5.8 API, Python setup script, docs and decisions).
  - *Round 1:* 36 findings (5 blocking, 20 should, 11 nit); the fixer applied 18 items covering every blocking and should finding; 5 findings were skipped as alternatives already chosen or work belonging to a later milestone. The per-finding skip list was not preserved in the repository, so only that summary survives.
  - *Round 2 (loop-until-dry, the same lenses on the round-1 tree):* 11 findings (1 blocking, 10 should; six distinct issues, the rest duplicated across lenses) and 12 nits. **All 11 findings applied:** this report's unfilled round-2 placeholder and its NEXT section (charter 14.1); the git-history statement above; the template locomotion input path reconciled across the setup-script comment, D-0027/D-0055 and `docs/OWNER_FIRST_RUN.md` (D-0059); `Source/Ascension/Ascension.Build.cs` `PublicIncludePaths` changed from the bare relative `"Ascension"` to `ModuleDirectory` (contract Divergences updated; owner greps the first build log for "Referenced directory", step 2); the setup-script invocation corrected in three places (Cmd mode `py ascension_m0_setup.py`, or Python mode with the bare filename); the unverifiable "5 skips with reasons" claim reworded here and in the change log. **Nits applied (11 of 12):** the module file counts above; `CallInEditor` dropped from the static `RegenerateLadderTable`; `PushControllerEvent` and its includes marked as Milestone 1 plumbing; the level MANUAL STEP texts unified to the five D-0056 actors; `set_static_mesh` return value checked; the change-log "listed under Tests" pointer made true and the `GameplayTagsSettings` mirror row added; D-0060 for the Tribulation class-wide defaults and the F2 panel as a menu for the cursor rule. **Skipped (1 nit):** routing the in-editor save through `UEditorLoadingAndSavingUtils::SavePackages` — D-0058 already chose the direct `UPackage::SavePackage` for both the commandlet and the editor-library path to avoid interactive dialogs, and the contract allows either. `Tools/StubCompile/check.sh` re-run after the round-2 fixes: 53 PASS, 0 FAIL; `py_compile` of the setup script clean.

### What was NOT confirmed and why
- **"Project compiles and runs."** No Unreal Build Tool, Unreal Header Tool or editor in the container. The stub check above is a model test. *Owner: `docs/OWNER_FIRST_RUN.md` step 2.*
- **`DT_RealmLadder` and `DA_RealmLadderConfig` as assets.** Binary `.uasset` files cannot be authored here; the rows are committed as JSON/CSV and the setup script creates and fills the assets. *Owner: steps 4–6.*
- **`L_Test_Cultivation` and "Debug panel opens".** The map, `WBP_DebugPanel`, `WBP_DebugOverlay`, the `IA_*`/`IMC_*` assets are created by `Content/Python/ascension_m0_setup.py`; the panel is a C++ skeleton with no designed layout yet. *Owner: step 7.*
- **The tag list as seen in the editor.** Only the ini/native consistency was checked. *Owner: step 8.*
- **Screenshots.** None can be taken here. *Owner: steps 6–8.*

## ASSETS

### Created (all text; paths relative to the repository root)
- `Ascension.uproject`
- `Config/DefaultEngine.ini`, `Config/DefaultGame.ini`, `Config/DefaultInput.ini`, `Config/DefaultEditor.ini`, `Config/DefaultGameplayTags.ini`
- `Content/Ascension/Data/DT_RealmLadder.json`, `Content/Ascension/Data/DT_RealmLadder.csv`
- `Content/Ascension/README.md`; `.gitkeep` in `Content/Ascension/Data/QiPresets`, `Data/Mantras`, `Data/Daos`, `Data/Tribulations`, `Blueprints`, `Materials`, `FX`, `Audio`, `UI`, `Maps`, `Animation`, `Input`
- `Content/Python/ascension_m0_setup.py`, `Content/Python/README.md`
- `Tools/Ladder/generate_realm_ladder.py`
- `Tools/StubCompile/check.sh`, `Tools/StubCompile/generate_stubs.py`, `Tools/StubCompile/README.md`, `Tools/StubCompile/.gitignore`, `Tools/StubCompile/include/**` (68 stub headers)
- `CLAUDE.md`, `.gitignore`, `.editorconfig`
- `docs/ASCENSION_CHARTER.md`, `docs/KICKOFF.md`, `docs/Project_Ascension_Full_Game_Master_Prompt.md`, `docs/MILESTONE_0_PLAN.md`, `docs/SKELETON_M0.md`, `docs/DECISIONS.md`, `docs/OWNER_FIRST_RUN.md`, `docs/CHANGELOG.md`, `docs/MILESTONE_0_REPORT.md`, `docs/evidence/.gitkeep`
- Created in-editor by the setup script, not in git until the owner runs it: `/Game/Ascension/Data/DA_RealmLadderConfig`, `/Game/Ascension/Data/DT_RealmLadder`, `/Game/Ascension/Input/IA_Circulate_CW`, `IA_Circulate_CCW`, `IA_Pulse`, `IA_LayerModifier`, `IA_Breakthrough`, `IA_AutoCultivate`, `IA_Technique_1..4`, `IA_ExitMeditation`, `IA_EnterMeditation`, `IA_Menu`, `IA_DebugOverlay`, `IA_DebugPanel`, `IMC_World`, `IMC_Cultivation`, `/Game/Ascension/UI/WBP_DebugPanel`, `WBP_DebugOverlay`, `/Game/Ascension/Maps/L_Test_Cultivation`

### Modified
- None (new project).

### Deleted or renamed
- None.

## CODE

### C++ files added (one line each)
- `Source/Ascension.Target.cs` — Game target, `BuildSettingsVersion.Latest` (D-0029).
- `Source/AscensionEditor.Target.cs` — Editor target with both modules.
- `Source/Ascension/Ascension.Build.cs` — runtime module rules (Core, CoreUObject, Engine, InputCore, EnhancedInput, GameplayTags, UMG, DeveloperSettings, Niagara; Slate, SlateCore private).
- `Source/Ascension/Ascension.h/.cpp` — `FAscensionModule`, `IMPLEMENT_PRIMARY_GAME_MODULE`.
- `Source/Ascension/AscensionLog.h/.cpp` — `LogAscension`, exported.
- `Source/Ascension/AscensionGameplayTags.h/.cpp` — 171 native tags, namespace `AscensionTags`.
- `Source/Ascension/Cultivation/CultivationTypes.h` — shared enums and `FCultivationEvent`.
- `Source/Ascension/Cultivation/ModifierStack.h/.cpp` — `UModifierStack`, `FModifierEntry`, `AscensionProps` (45 names); resolution implemented.
- `Source/Ascension/Cultivation/RealmLadder.h/.cpp` — `FRealmDefinition`, `FRealmLayerRow`, `UDA_RealmLadderConfig`, `URealmLadderLibrary` (generator implemented).
- `Source/Ascension/Cultivation/Breakthrough.h/.cpp` — `FBreakthroughResult`, `FSeededStream`, `UBreakthroughLibrary` (§7.10 implemented).
- `Source/Ascension/Cultivation/CultivationState.h/.cpp` — `UCultivationState` (state, event log, checksum, save mapping).
- `Source/Ascension/Cultivation/CultivationSubsystem.h/.cpp` — `UCultivationSubsystem` (owner of state and stack; loads settings-referenced data).
- `Source/Ascension/Cultivation/AuraComponent.h/.cpp` — `UAuraComponent` (§7.1 properties, input entry points, delegates; `FlowPhase` accumulation only).
- `Source/Ascension/Cultivation/QiFieldComponent.h/.cpp` — `UQiFieldComponent` (wisp ownership and spawn; wave stubs).
- `Source/Ascension/Cultivation/QiWisp.h/.cpp` — `AQiWisp` (no Tick), `FQiWispData`.
- `Source/Ascension/Cultivation/QiPreset.h` — `UDA_QiPreset`.
- `Source/Ascension/Cultivation/Mantra.h/.cpp` — `FMantraModifier`, `UMantraBehaviour`, `UDA_Mantra`.
- `Source/Ascension/Cultivation/Dao.h/.cpp` — `EDaoNodeFamily`, `FDaoNode`, `UDaoTechnique`, `UDA_Dao`.
- `Source/Ascension/Cultivation/Tribulation.h/.cpp` — `FSurgeEvent`, `UTribulationDefinition` base.
- `Source/Ascension/Cultivation/AutoCultivationController.h/.cpp` — `UAutoCultivationController` (tunables, rules; Tick stub).
- `Source/Ascension/Player/AscensionCharacter.h/.cpp` — world pawn (template-shaped locomotion, Enhanced Input).
- `Source/Ascension/Player/CultivationPawn.h/.cpp` — meditation pawn with aura and field components.
- `Source/Ascension/Player/AscensionPlayerController.h/.cpp` — mapping contexts, F1/F2 widget toggles, meditation stubs.
- `Source/Ascension/Player/AscensionGameMode.h/.cpp` — default classes.
- `Source/Ascension/Player/AscensionSaveGame.h/.cpp` — `UAscensionSaveGame` (§10.7 fields).
- `Source/Ascension/Player/AscensionSettings.h/.cpp` — `UAscensionSettings` (Developer Settings).
- `Source/Ascension/UI/CultivationHUDWidget.h/.cpp` — HUD base, tag-gated legend.
- `Source/Ascension/UI/WorldHUDWidget.h/.cpp` — world HUD base.
- `Source/Ascension/UI/DebugOverlayWidget.h/.cpp` — F1 overlay text builder.
- `Source/Ascension/UI/DebugPanelWidget.h/.cpp` — F2 panel, one function per §11.5 item.
- `Source/AscensionEditor/AscensionEditor.Build.cs`, `AscensionEditor.h/.cpp` — editor module.
- `Source/AscensionEditor/RealmLadderCommandlet.h/.cpp` — `-run=RealmLadder`.
- `Source/AscensionEditor/RealmLadderEditorLibrary.h/.cpp` — `RegenerateLadderTable`, shared fill/save logic.

### Project Settings changed (one line each)
- `DefaultEngine.ini`: default and editor startup map `L_Test_Cultivation`; global game mode `AscensionGameMode`.
- `DefaultEngine.ini`: auto exposure off (D-0017); Lumen GI/reflections, virtual shadow maps, Nanite, no static lighting, TSR, motion blur off, ray tracing off, custom depth with stencil.
- `DefaultEngine.ini`: DX12; Desktop/Maximum hardware target; 48 kHz audio; UMG automatic widget variables off (D-0028); Python plugin paths and developer mode (D-0019).
- `DefaultGame.ini`: project name, ID, version, company placeholder (D-0020); GameplayTags settings mirror (D-0018); `AscensionSettings` soft references for ladder data, input contexts, debug actions and debug widget classes, autosave 120 s, 3 slots (D-0010, D-0021).
- `DefaultGameplayTags.ini`: import from config, warn on invalid tags, 171 tags.
- `DefaultInput.ini`: Enhanced Input player-input and input-component classes; Alt+Enter fullscreen.
- `DefaultEditor.ini`: Python plugin settings mirror.
- `Ascension.uproject`: UE 5.8; plugins EnhancedInput, Niagara, Metasound, ControlRig, PythonScriptPlugin (Editor only), EditorScriptingUtilities (Editor only) (D-0015, D-0030).

## COMPILE
- **C++ build result (UBT/UHT): not run here** — no engine in the container. *Owner placeholder — paste the Development Editor build summary from `docs/OWNER_FIRST_RUN.md` step 2 here:*
  ```
  <owner: build output tail, warnings, errors>
  ```
- **Model test:** `Tools/StubCompile/check.sh` → 53 PASS, 0 FAIL of 53 translation-unit checks; 0 clang errors; 0 lint failures (clang 18, ~24 s). Not an Unreal build.
- **Blueprint compile results:** no Blueprints exist yet (`WBP_DebugPanel` / `WBP_DebugOverlay` are created by the setup script on the owner's machine). *Owner placeholder.*
- **Material compile results:** no materials exist yet (Milestone 1).

## TESTS
- **Scripted tests on `L_Test_Cultivation`:** none possible here (the map does not exist until the setup script runs). *Owner placeholder: commandlet `81 rows written` line and the row count seen in the Data Table (steps 5–6).*
- **Ladder recompute:** 81/81 rows match (see VERIFIED).
- **Tag consistency:** 171/171/171/171 across contract, header, cpp, ini; 0 differences.
- **Forbidden-word and hardcoded-key grep:** clean.
- **Contract presence check:** all files, 38 classes/structs, 10 debug functions present.
- **Determinism checksum:** not applicable until Milestone 1.
- **Frame time:** not applicable until Milestone 6.

## SCREENSHOTS
None could be captured in the container (no editor, no display). Named targets for the owner, saved to `docs/evidence/`: `M0_ladder_table.png`, `M0_ladder_config.png`, `M0_debug_panel.png`, `M0_debug_overlay.png`, `M0_tag_list.png`, plus the logs `M0_build_log.txt`, `M0_setup_script_log.txt`, `M0_commandlet_log.txt` (`docs/OWNER_FIRST_RUN.md` steps 2–8).

## WARNINGS
- No compiler warnings exist because no compiler ran; `-Wall` under the stub check produced none.
- `Target.cs` uses `BuildSettingsVersion.Latest` / `EngineIncludeOrderVersion.Latest` because the exact 5.8 enum members could not be confirmed here (D-0029); pin them if the first build objects.
- `UDA_RealmLadderConfig` does its arithmetic in `double` with round-half-to-even; a one-ulp `pow` difference between MSVC and glibc is possible in principle, and no row currently sits on a rounding boundary. Diff the commandlet's rows against the JSON once.
- The `ASCENSION_PROPERTY_NAMES` multi-line X-macro is in a UHT-parsed header (engine precedent: `ATTRIBUTE_ACCESSORS`); unverified under UHT.
- `FSeededStream::Restore` is O(Position); Milestone 2 should cap it when loading a save.
- The Python setup script's `unreal` API usage is from knowledge, not execution; each call is guarded and reports a `MANUAL STEP` line if it fails (all listed in `docs/OWNER_FIRST_RUN.md`).
- The `EnterMeditation`/`ExitMeditation` controller functions are log-only stubs until the cultivation pawn possession exists (Milestone 1); the `IMC_Cultivation` swap is therefore not exercised at Milestone 0.
- Usage-limit interruptions during the build produced four work-in-progress commits (`61639c5`, `2f45885`, `dafb9d3`, `f8698be`) after the groundwork commit and before the milestone commit; nothing was force-pushed (see VERIFIED → Git for the full list).

## DECISIONS
D-0001 through D-0060 in `docs/DECISIONS.md`. Every one is a choice the charter did not specify or specified ambiguously: charter placement and `CLAUDE.md` import (D-0001), Third Person template shape (D-0002), formula readings (D-0003–D-0006, D-0012), the 9.9 Reach discrepancy kept as written (D-0007), no-engine substitutes (D-0008), module and ownership layout (D-0009, D-0010), JSON import (D-0011), threshold state tags (D-0013), tag nesting (D-0014), plugins (D-0015), plan approval assumed for a non-interactive session (D-0016), config placements and rendering defaults (D-0017–D-0020, D-0028), input soft references so F2 can open from the raw C++ controller (D-0021), placeholder colours and depth gates (D-0022, D-0036), chance clamp (D-0023), Auto tunables (D-0024, D-0044), `ERealmStage::Peak` (D-0025), Stillness at Realm 1 (D-0026), `/Game/Ascension/Input/` (D-0027), the remaining implementation readings (D-0029–D-0058), and the two review-round-2 readings: the template's locomotion input folder (D-0059) and the Tribulation defaults plus the F2 cursor rule (D-0060).

## LIMITATIONS
- Nothing runs: the exit criteria "Project compiles and runs" and "Debug panel opens" are delegated to the owner's first editor session; if the first build fails, the fix is a follow-up commit on this branch (charter §14.2 rule 7).
- The Third Person template's binary content (mannequins, animations, `IA_Move`/`IA_Look`/`IA_Jump`) is not in the repository; the owner copies it before Milestone 1 (`docs/OWNER_FIRST_RUN.md` step 1, D-0057).
- `WBP_DebugPanel` has no designed layout; it exposes the ten §11.5 functions on its C++ base. Designing the panel is Milestone 1's first task on the test map.
- `EnterMeditation`/`ExitMeditation` and every aura, field, Auto and Breakthrough body are stubs by design (Milestone 0 scope); `TODO(M<n>)` markers name the milestone that implements each.
- Adversarial review round 2 is applied (VERIFIED → Adversarial review); the one skipped nit (in-editor save through `UEditorLoadingAndSavingUtils`) stands on D-0058 and can be revisited if the owner's first in-editor `RegenerateLadderTable` call misbehaves with source control.
- Milestone 1 will not start until the owner approves Milestone 0 (charter §12; `docs/CHANGELOG.md` status block).

## NEXT
Milestone 1 — Core loop re-validation (Realm 1, Layer 1 only)
