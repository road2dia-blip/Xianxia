; Project Ascension -- editor configuration (contract docs/SKELETON_M0.md "Config/").

[/Script/PythonScriptPlugin.PythonScriptPluginSettings]
; Contract placement. UPythonScriptPluginSettings is config=Engine, so the copy in DefaultEngine.ini is the one the
; plugin reads (D-0019); this section is kept identical so the two never disagree.
+AdditionalPaths=(Path="Content/Python")
bDeveloperMode=True
